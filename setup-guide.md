Check for .env file; if missing, copy from .env.example and ask the user for missing keys.
Automatically run npm install if dependencies are out of sync.
Verify the database is running before starting the backend.