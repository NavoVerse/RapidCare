How to use it:
You can test the Windows app by running:

powershell:
cd windows_app
npm start

If you ever want to package the app into a standalone .exe installer, you can run:

powershell:
npm run build